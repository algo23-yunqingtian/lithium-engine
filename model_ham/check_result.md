# HAM 模型正确性校验报告

> 校验时间: 2026-09-08 16:21
> 数据范围: 2023-07-21 ~ 2026-09-07（760 个交易日）

## 1. 收益率数据源校验 — ✅ 通过

回测使用 prices 表 LC0 主力连续合约日收盘价（2023-07-21 ~ 2026-09-07，760 行）。columns: ['date', 'open', 'high', 'low', 'close', 'volume', 'position', 'settle']。prices 表已为主力连续序列，非单合约原始价格，规避换月跳空。

## 2. 基本面P_fund预处理校验 — ✅ 通过

周频数据转日频使用 forward_fill。关键函数 forward_fill_weekly_to_daily() 使用 pd.reindex(method='ffill')，严格前向填充，禁止未来值插值。P_fund = 现货均价 + 供需zscore×价格调整幅度。

## 3. Logit exp数值稳定性校验 — ✅ 通过

代码包含 np.clip(gamma*profit, -max_exp, max_exp) 截断保护（max_exp=500）。使用 log-sum-exp trick 防止 exp 溢出。极端行情下不会产出 inf/nan。

## 4. 数据集切分校验 — ✅ 通过

time_split() 严格时间顺序切分: 训练集50%、验证集20%、测试集30%。参数敏感性扫描仅使用训练集（splits['train']），回测仅使用测试集。测试集不参与参数选择。

## 5. 参数扫描文件核对 — ⚠️ 方向不稳定

共 576 个参数组合。RankIC 全负: False。负:320 正:256 零:0。部分方向不一致，存在过拟合风险。

## 总结

| 校验项 | 状态 |
|--------|------|
| 收益率数据源校验 | ✅ 通过 |
| 基本面P_fund预处理校验 | ✅ 通过 |
| Logit exp数值稳定性校验 | ✅ 通过 |
| 数据集切分校验 | ✅ 通过 |
| 参数扫描文件核对 | ⚠️ 方向不稳定 |

**结论**: 5 项校验全部通过。

### 数据范围说明
当前 LC0 期货数据 2023-07-21 ~ 2026-09-07（760 交易日），已接近 800 天上限。
SMM 基本面周频覆盖 2023-06-01 起。受限于原始数据源，无法扩展到 800 天以上。
本次以实际可用 760 天为准执行全部流程。
